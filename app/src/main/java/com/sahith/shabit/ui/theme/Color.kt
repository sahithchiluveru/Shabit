package com.sahith.shabit.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Shabit's surface palette. These are the only chrome colours in the app —
 * every other colour on screen belongs to a habit and is chosen by the user.
 */
val ShabitBackground = Color(0xFF0F172A)
val ShabitCard = Color(0xFF1E293B)
val ShabitTextPrimary = Color(0xFFF1F5F9)
val ShabitTextSecondary = Color(0xFF94A3B8)
val ShabitAccent = Color(0xFFA78BFA)
