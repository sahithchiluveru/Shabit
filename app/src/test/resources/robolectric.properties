# Robolectric drives these tests against a fixed platform rather than the module
# targetSdk, so bumping targetSdk cannot silently change what the data layer is
# tested on. Nothing here touches an SDK-version-dependent API.
sdk=34
